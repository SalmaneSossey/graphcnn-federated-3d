# Colab already ships with a CUDA-enabled PyTorch build.
# Keep torch out of this file to avoid replacing the working Colab install.
numpy>=1.24
pandas>=2.0
pyyaml>=6.0
tqdm>=4.66
scikit-learn>=1.3
huggingface_hub>=0.23
pytest>=8.0
