seed: 42
device: auto
teacher_checkpoint: checkpoints/teacher.pt
student_checkpoint_dir: checkpoints/distillation
num_classes: 10
input_channels: 6
student_hidden_dim: 32
epochs: 20
batch_size: 32
learning_rate: 0.001
alpha_values: [0.3, 0.5, 0.7]
temperature_values: [2.0, 4.0]

