# Metadata

Place `labeled_dataset.csv` here:

```bash
data/metadata/labeled_dataset.csv
```

The CSV is intentionally ignored by Git and should be tracked with DVC:

```bash
bash scripts/add_labeled_dataset_to_dvc.sh
```

